{
	int a, b, e;
	float c, d;

	print("Valor de A:");
	read(a);
	print("Valor de B:");
	read(b);
	while (a != b)
	{
		if (a > b)
		{
			a = a - b;
		}
		if (b > a)
		{
			b = b - a;
		}
	}
	a = b * 2 - 1 + c;
	print("\nRes=");
	print(a);
}
